import { View, Button } from "react-native-ui-lib";

const HomeScreen = () => {
    return (
        <View>
            <Button label='Next'></Button>
        </View>
    );
};

export default HomeScreen;
